"use client";
import LeftSide from "@/components/LeftSide";
import Navbar from "@/components/Navbar";
import Head from "next/head";
import Image from "next/image";
import RightSide from "@/components/RightSide";
import BgSwirlVideo from "@/components/bgSwirlVideo";
import { motion } from "framer-motion";
import TextBanner from "@/components/TextBanner";
import About from "@/components/About";
import Services from "@/components/Services";
import Resources from "@/components/Resources";
import How from "@/components/How";
import Contact from "@/components/Contact";

function Home() {
  return (
    <>
      <Head>
        <title>U SEE US</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className="w-full font-bodyFont bg-bodyColor text-textLight overflow-x-hidden overflow-y-scroll ">
        <motion.div>
          <Navbar />
          <BgSwirlVideo />
        </motion.div>
        <div className="z-10 w-full h-screen flex flex-col justify-center align-center items-center"></div>

        <div className="w-full xl:flex items-center justify-between">
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.7 }}
            className="hidden xl:inline-flex w-32 h-full fixed left-0 bottom-0"
          >
            <LeftSide />
          </motion.div>

          <div className="w-full h-full flex flex-col items-center">
            <Services />
            <How />
            <Contact />
          </div>
          {/* Contact */}
          {/* Footer */}

          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.7 }}
            className="hidden xl:inline-flex w-32 h-full fixed right-0 bottom-0"
          >
            <RightSide />
          </motion.div>
        </div>
      </main>
    </>
  );
}
export default Home;
